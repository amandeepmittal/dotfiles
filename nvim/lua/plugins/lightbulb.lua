return {
  "kosayoda/nvim-lightbulb",
  opts = {
    autocmd = { enabled = true },          -- check for code actions when the cursor rests
    sign = { enabled = true, text = "󰌵" }, -- nerd-font bulb; emoji is double-width in the gutter
  },
}
